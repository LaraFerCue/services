version: "3.4"

volumes:
  portainer_data:
    name: "portainer_data"
services:
  portainer:
    image: "portainer/portainer-ce"
    ports:
      - "8000:8000"
      - "9443:9443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
