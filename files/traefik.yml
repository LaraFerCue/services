entryPoints:
  web:
    address: :80
      http:
        redirections:
          entryPoint:
            to: websecure
            scheme: https
      websecure:
        address: :443

tls:
  certificates:
    - certFile: /etc/traefik/barbunicorn.gay.crt
    - keyFile: /etc/traefik/barbunicorn.key
